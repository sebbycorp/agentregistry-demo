# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

services:
  platform-ops-agent:
    image: ghcr.io/platform-ops-agent:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["platform_ops_agent", "--local"]
    ports:
      - "8080:8080"
    environment:
      # TODO: remove kagent variables (currently required)
      - KAGENT_URL=placeholder
      - KAGENT_NAME=placeholder
      - KAGENT_NAMESPACE=placeholder
      - AGENT_NAME=platform-ops-agent
      - OTEL_SERVICE_NAME=platform-ops-agent
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT}
      - MODEL_PROVIDER=openAI
      - MODEL_NAME=gpt-4o
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - type: bind
        source: ./platform_ops_agent
        target: /config
