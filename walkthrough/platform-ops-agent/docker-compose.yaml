# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

services:
  platformopsagent:
    image: ghcr.io/platform-ops-agent:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["platformopsagent", "--local"]
    ports:
      - "8080:8080"
    environment:
      # TODO: remove kagent variables (currently required)
      - KAGENT_URL=placeholder
      - KAGENT_NAME=placeholder
      - KAGENT_NAMESPACE=placeholder
      - AGENT_NAME=platformopsagent
      - OTEL_SERVICE_NAME=platformopsagent
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT}
      - MODEL_PROVIDER=openAI
      - MODEL_NAME=gpt-4o
      - KAGENT_SKILLS_FOLDER=/skills
    volumes:
      - type: bind
        source: ./platformopsagent
        target: /config
      - type: bind
        source: ./platformopsagent/skills
        target: /skills
        read_only: true
